#include <unordered_map>
#include <vector>

using namespace std;

class Solution {
public:
    int countKDifference(vector<int>& nums, int k) {
        // a counter map
        unordered_map<int, int> counter;

        // declare the result integer variable
        int result = 0;


        // loop through the elements of nums and print it 
        
        
        
    }
};